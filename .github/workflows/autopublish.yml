name: Autopublish
on:
  push:
    branches:
      - main

permissions:
  contents: read
  packages: write

jobs:
  publish:
    name: Publish GitHub Package
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          registry-url: https://npm.pkg.github.com
          scope: '@xitij'
      - name: Install Dependencies
        run: npm ci
      # JS-DevTools/npm-publish disables lifecycle scripts by default, so
      # prepublishOnly (webpack) never runs. Build the bundles explicitly.
      - name: Build bundles
        run: npm run build
      - name: Publish
        uses: JS-DevTools/npm-publish@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          registry: https://npm.pkg.github.com
          strategy: all
